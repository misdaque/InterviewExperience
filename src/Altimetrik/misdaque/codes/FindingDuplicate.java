package Altimetrik.misdaque.codes;

import java.util.List;

/*
* Given a list of integers, return a list of duplicates element using streams
* */

public class FindingDuplicate {

    public static void main(String[] args) {

        List<Integer> lists = List.of(10,15,8,49,25,98,98,32,15);

//        List<Integer> integers = lists.stream()
//                        .filter(num -> lists.stream().count())

//        System.out.println(integers);
    }
}
